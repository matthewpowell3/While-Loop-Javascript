///While Loop Javascript
var x = 1;
while (x <= 5) {
  document.write('<h3>' + x + '</h3>');
  // without a increment/decrement it will go in to a infinite loop
  // console.log(x);
  x += 2; // x=x + 2 increment by two
}
